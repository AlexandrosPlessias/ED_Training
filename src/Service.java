/**
 * Here will implemented the CRUD methods for
 * (Spitter)??? & Spittle class/es.
 */
public class Service {

    // I guess create method will create/generate Spittle objs.
    public static Spittle create( /*init val's of obj if exists*/ ){
        // call constructor() of Spittle's obj.
        return null; // will be replaced with Spittle
    }

    // i will need the obj
    public static void read(/* Spittle sp */){
        // call toString() of Spittle's obj.
    }

    // i will need the obj and the elements.
    public static void update(/*obj, element*/){
        // call setter() of Spittle's obj.
        System.out.println("Updated successfully!");
    }

    // i will need the obj and the elements.
    public static boolean delete(/*obj, element*/){

        //if (element exist) {perform delete; System.out.println("{Element} deleted successfully!");}
        // else System.err.println("Delete Fails!");
        return false;
    }
}
